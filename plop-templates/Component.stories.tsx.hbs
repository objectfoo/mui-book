import type { Meta, StoryObj } from "@storybook/react-vite";
import { {{ pascalCase ComponentName }}, type {{ pascalCase ComponentName }}Props } from "./{{ pascalCase ComponentName }}";

const meta = {
	title: "Section Name Missing/{{ pascalCase ComponentName }}",
	component: {{ pascalCase ComponentName }},
	tags: [],
	parameters: { },
	argTypes: {
		label: { type: "string" },
	},
} satisfies Meta<{{ pascalCase ComponentName }}Props>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
	args: {
		label: "label",
	},
};
