import type { Meta, StoryObj } from "@storybook/react-vite";
import { {{ camelCase ComponentName }}, type {{ camelCase ComponentName }}Props } from "./{{ camelCase ComponentName }}";

const meta = {
	title: "Data Display/{{ camelCase ComponentName }}",
	component: {{ camelCase ComponentName }},
	tags: ["autodocs", "data-display"],
	parameters: {
		layout: "centered",
	},
	argTypes: {
		label: { type: "string" },
		color: {
			options: [
				"primary",
				"secondary",
				"textPrimary",
				"textSecondary",
				"info",
				"error",
				"success",
				"warning",
			] satisfies {{ camelCase ComponentName }}Props["color"][],
		},
	},
} satisfies Meta<{{ camelCase ComponentName }}Props>;

export default meta;

type Story = StoryObj<typeof meta>;

// MARK: Stories
export const Default: Story = {
	args: {
		label: "label",
		color: "textPrimary",
	},
};
